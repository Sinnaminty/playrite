# Sample story

Open [The last light](the-last-light.html) in a browser to see the standalone reader.

The sample can be recreated in the editor with `playrite demo sample.playrite` and exported with Ctrl+E.
